#:kivy 1.0

BoxLayout:
    orientation: 'horizontal'

    BoxLayout:
        orientation:'vertical'
        size_hint: (.7, 1)

        Label:
            text: 'videoplay section'
            size_hint: (1, .4)
        Label:
            text: 'soundwave section'
            size_hint: (1, .6)

    Label:
        text: 'caption section'
        size_hint: (.3, 1)